import type { TINtertationConfig } from './types'

/**
 * Defines the configuration for the Intertation parser
 */
export function defineConfig(config: TINtertationConfig): TINtertationConfig {
  return config
}
